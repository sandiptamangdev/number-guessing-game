# **Number Guessing Game (Node.js CLI)**

A simple command-line number guessing game built with Node.js.
You have **10 attempts** to guess a **random number between 1 and 100**.
After each guess, the game tells you whether you should pick **higher** or **lower**.

---

## 🚀 **Features**

* Runs entirely in the terminal (CLI).
* Uses Node’s built-in `readline` module.
* Random number from **1 to 100**.
* **10 attempts** with instant hints after each guess.
* Auto-ends when the correct number is found.

---

## 📦 **Requirements**

* Node.js installed (any recent version).

---

## ▶️ **How to Run**

1. Clone or download the repo.
2. Open the folder in your terminal.
3. Run:

```
node your-file-name.js
```

4. Follow the on-screen instructions to play.

---

## 🛠️ **Current Status**

* Fully playable.
* Large callback chain (refactor in progress).
* Target: convert to a loop-based or modular function-based system.

---

## 📌 **Future Improvements**

* Replace nested callbacks with cleaner logic.
* Add input validation.
* Add replay option.
* Improve structure for readability.

---
